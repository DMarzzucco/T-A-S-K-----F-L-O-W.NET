# API AUTH SECURITY IMPLEMENTS

## Install

```bash
#Init the db
docker-compose up db 

#Generate the table
npx prisma generate

# Migrate the deploy
npx prisma migrate deploy

# Start the server
npm run start:dev

```